
version: 0.1.{build}

image:
  - Visual Studio 2019

environment:
  CMAKE_GENERATOR: "Visual Studio 16 2019"
  matrix:
    # x86
    - CMAKE_GENERATOR_PLATFORM: x86
    
    
    # x64
    - CMAKE_GENERATOR_PLATFORM: x64


before_build:
 - cmd: cmake -H. -B./build

build:
  verbosity: normal
